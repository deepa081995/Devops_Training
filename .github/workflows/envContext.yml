name: Env Context

on:
  workflow_dispatch
env:
  Var1: workflow
jobs:
  env_context:
    env:
      var1: jobs
    runs-on: ubentu-latest
    steps:
    - name: Print
      run: echo '${{env.var1}}'
      env:
        var1: steps
    
      
